[profile.release]
lto = "fat"
codegen-units = 1

[workspace]
members = ["crates/bins/*", "crates/libs/types"]
resolver = "2"

[workspace.dependencies]
clap = { version = "^4.5.60", features = ["derive"] } # unified

[workspace.package]
rust-version = "1.87.0"
version = "0.1.0"
edition = "2024"
